[mcu mmu1]
canbus_uuid: 32f87fe86487
canbus_interface: can0 

[mcu mmu0]
canbus_uuid: 36236be06596  
canbus_interface: can0

[mcu mmu2]
canbus_uuid: c4905987b27f  
canbus_interface: can0

[board_pins mmu]
mcu: mmu0, mmu1, mmu2
aliases:
    MMU_GEAR_UART=PB6,
    MMU_GEAR_STEP=PB5,
    MMU_GEAR_DIR=PB4,
    MMU_GEAR_ENABLE=PB7,
    MMU_GEAR_DIAG=,

    MMU_NEOPIXEL_BOX=PA2,
    MMU_NEOPIXEL_BUTTON=PA4,

    MMU_PRE_GATE=PA1,
    MMU_POST_GEAR=PA0,

    MMU_TENSION=PB1, # First lane has the sync feedback sensor attached to it!
    MMU_COMPRESSION=PB0, # First lane has the sync feedback sensor attached to it!
    MMU_TH=PA3, # First lane has the PFS sensor attached to it!

    MMU_FAN=PA15,
    EJECT_BUTTON=PC6,